[build]
builder = "NIXPACKS"

[build.nixpacks]
node_version = "18"

[deploy]
buildCommand = "npm install && cd server && npm install && cd ../mcp-server && npm install && cd .. && npm run build"
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"